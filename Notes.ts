/*
HTTP Cookies
string - key:value
returned to browser
maintain a session
request header (there might be cookies)
response header(there might be cookies)
usual session request login endpoint(1) authenticate user, 2) Send response with cookie)

--jwt token
Security, same idea as cookies
Encrypted text(jwt) within cookie, could contain a payload. base64 encryption that can be signed.(only server can reveal content)

*/